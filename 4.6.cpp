#include <iostream>
#include <cmath>
using namespace std;
const int N = 31270;
long long int a[N],s[N];
void MakeTable() {
    //????????????????,??????????,??????,??????????,????
    a[1] = s[1] = 1;
    for(int i = 2; i < N; i++) {
        //(int)log10((double)i)+1???????????????:
        //?111,??2+1?
        a[i] = a[i - 1] + (int)log10((double)i) + 1;
        //????????,??????
        s[i] = s[i - 1] + a[i];
        //????????,??????
    }
}
int FindNum(int n) {
    int i = 1;
    while(s[i] < n) {
        i++;//?????????
    }
    int locat = n - s[i - 1];
    //??????????,???????????,??n->locat
    int len = 0;
    for(i = 1; len < locat; i++) {
        len += (int)log10((double)i) + 1;
        //??????len?locat??,?len??????locat?????
    }
    return ((i - 1) / (int)pow((double)10, len - locat)) % 10; 
    //len-locat???????????????????
    //(int)pow((double)10,len-locat)??????10????0???
    //(i-1)/(int)pow((double)10,len-locat)?????????????,??????????????
    //······%10??????????,????,????
}
int main() {
    MakeTable();
    int n;
    cin >> n;
    while(n--) {
        int num;
        cin  >> num;
        cout << FindNum(num) <<endl;
    }
    return 0;
}
